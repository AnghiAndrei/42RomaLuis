[X] Prendere in input una stringa
[X] Cronologia dei comandi
[X] Avviare comandi, esempio: ls
[X] Avviare comandi, esempio: /bin/ls
[Y] Stampa le variabili globali, esempio: $HOME. Nota: una var d'ambiente e composto da charatteri alphamunnetici e _; Nota2: lui espande a prescindere sia che esiste, sia no
[X] Stampa codice uscita dell'ultimmo comando: $?
Gestire ' ":
    [X] ' prende il contenuto delle ' e lo usa cosi come
    [X] " prende il contenuto delle " e lo usa cosi come, tranne la var. ambiente
    [X] se non sono stati chiusi: '
    [X] se non sono stati chiusi: "
Redirect:
    [X] < ridireziona l'input
    [X] > ridireziona l'output sostituendo il contenuto
    [X] >> ridireziona l'output in modo append
    [X] << fine leggere dallo standart input fino all'inserimento del delimitatore
    [X] | l'uotput del comando diventa l'input del prossimo comando
Gestire segnali, quando aspetta dei comandi:
    [X] ctrl-C cancella il contenuto della riga
    [X] ctrl-D esce dalla shell
    [X] ctrl-\ non fa nulla
Builtins:
	NOTA: SE SI CANCELLA PATH QUESTI DEVONO CONTINUARE A FUNZIONARE
    [X] env Mostra tutte le variabili d'ambiente con all'interno dei valori
    [Y] pwd Mostra in quale cartella ti trovi con il terminale
    [Y] unset Rimuove una variabile d'ambiente
    echo
		[X] echo testo Stampa il testo con \n
		[Y] echo "  testo  " Stampa il testo con \n
		[Y] echo '  testo  ' Stampa il testo con \n
		[X] echo -n testo Stampa il testo senza il \n
    cd Ti cambia di directori del terminale
		[-] cd  Ti porta all'inizio delle cartelle
		[-] cd .. Ti porta alla cartella precedente
		[-] cd cartella Ti porta alla cartella indicata
    export
		[X] export Stampa tutte le variabili d'ambiente, in ordine alfabetico
		[Y] export var Aggiunge la variabile d'ambiente senza valore
		[Y] export var=val Aggiunge la variabile d'ambiente con valore
		[Y] export var=val Assegna il valore, alla variabile esistente
		[Y] export var+=val Aggiunge la variabile d'ambiente con valore
		[Y] export var+=val Aggiunge il valore alla variabile d'ambiente
		[Y] export var+="gestisco i '"
		[Y] export var+='gestisco i "'
    exit
		[X] Esce dalla shell
		[X] Non esce dalla shell se in una pipe
		[X] exit 42 Esce dal programma con quel codice di errore
		[X] Controllare se sia passsato un numero

Bonus:
	[X] Implementa: && esegue entrambi i comandi
	[X] Implementa: || esegue il secondo comando solo se il precedente ha dato esito positivo
	[ ] Implementa: le ()
	[Y] Implementa: wildcard *

Casi limite:
	[ ] echo 1 2 > hola outfile
	[ ] echo "'"$HOME"'"
	[ ] "ls" esegue il comando
	[ ] 'ls' esegue il comando
Dare errore:
	[ ] 'ls -la' non riconosce il comando
	[ ] "ls -la" non riconosce il comando
	[ ] cmd &&& cmd
	[ ] |, &&, ||, non chiusi
	[ ] cmd && && cmd
	[ ] cmd || || cmd
Non fa nulla:
	[X] Nessun valore
	[X] Solo spazi
